{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "染谱主窗口默认权限",
  "windows": ["main", "print-preview"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:app:default",
    "dialog:default",
    "updater:default",
    "process:default"
  ]
}
